package com.unknown.host;

import java.util.List;

/** Media as a collection -- element type is erased, so only the generic signature shows it. */
public class ListTweet {
    public List<com.twitter.media.av.model.Entity> media;
}
