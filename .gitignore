*.iml
.gradle
/local.properties
/.idea
.DS_Store
# Not /build: this is a single-module project whose output lands in app/build, and anchoring
# the pattern to the root missed it entirely.
build/
/captures
.externalNativeBuild
.cxx
local.properties
